[pytest]
asyncio_mode = auto
; A single event loop for the whole test session avoids the asyncpg/redis
; "bound to a different event loop" errors that show up when async clients
; created at import time are reused across pytest-asyncio's default
; per-test loops.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
